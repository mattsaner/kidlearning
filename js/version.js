// VERSION is bumped by hand for releases. BUILD is replaced by the deploy
// workflow with the short commit SHA and date, so the live site shows exactly
// which commit is deployed ("dev" when running locally).
export const VERSION = '1.0.0';
export const BUILD = 'dev';
